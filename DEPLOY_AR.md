# ربط الموقع بقاعدة البيانات على Cloudflare (D1) ☁️ — خطوة بخطوة

لو الموقع شغال بس **مش متصل بالداتابيز** (التعديلات بتتحفظ على جهازك بس ومش بتظهر
على الأجهزة التانية)، امشي على الخطوات دي بالترتيب.

> 💡 أسهل طريقة تعرف بيها المشكلة فين: افتح في المتصفح
> `https://<اسم-موقعك>.pages.dev/api/health`
> الصفحة دي هتقولك بالظبط إيه اللي ناقص. ولوحة الأدمن فيها شريط حالة بيعرض نفس الكلام.

---

## الخطوة 1: إنشاء قاعدة البيانات (لو لسه ما اتعملتش)

من التيرمينال (بعد `npx wrangler login`):

```bash
npx wrangler d1 create batoot
```

الأمر هيطبع `database_id` — انسخه وحطه في ملف `wrangler.toml` مكان الأصفار.
(الـ id ده لازم للأوامر اللي بتتنفذ على السحابة فقط.)

## الخطوة 2: إنشاء الجداول على الداتابيز البعيدة ⚠️ (أكتر خطوة بتتنسي)

```bash
npx wrangler d1 execute batoot --remote --file=./schema.sql
```

لازم تشوف `success: true` في النتيجة. من غير الخطوة دي الـ API هيرد بخطأ
`no such table` والموقع هيشتغل في **الوضع المحلي** من غير ما يبين خطأ واضح.

## الخطوة 3: ربط الداتابيز بمشروع Pages

1. افتح [Cloudflare Dashboard](https://dash.cloudflare.com/) ←
   **Workers & Pages** ← مشروع `batoot` ← **Settings** ← **Bindings** ← **Add** ← **D1 database**
2. اسم المتغير (Variable name): `DB` — لازم بالظبط كده (حرفين كابيتال)
3. اختار قاعدة البيانات `batoot`
4. ⚠️ مهم جدًا: اربطها على بيئة **Production** (مش Preview بس)
5. بعد الربط لازم تعمل **إعادة نشر**: Deployments ← آخر deployment ←
   **Retry deployment** (أو اعمل push جديد على GitHub)
   > الربط الجديد مش بيشتغل على النشرة القديمة — لازم redeploy.

## الخطوة 4: تحميل الكتالوج الافتراضي (مرة واحدة بس)

الجداول دلوقتي موجودة لكن **فاضية**. حمّل الـ 10 منتجات والإعدادات بإحدى الطريقتين:

**الطريقة الأسهل (من الموقع نفسه):**
1. افتح موقعك ← دوس على أيقونة الدرع 🛡️ ← ادخل PIN الأدمن
2. هتلاقي شريط أخضر: **Cloud Database Connected ☁️**
3. دوس زرار **Load Default Catalog** ← أكّد ← استنى رسالة النجاح

**أو من التيرمينال:**

```bash
curl -X POST https://<اسم-موقعك>.pages.dev/api/seed
```

## الخطوة 5: التأكد إن كله شغال ✅

1. افتح `https://<اسم-موقعك>.pages.dev/api/health` — لازم تشوف:
   ```json
   { "ok": true, "db": true, "counts": { "products": 10, ... } }
   ```
2. افتح لوحة الأدمن — لازم الشريط يكون أخضر ومكتوب عدد المنتجات.
3. جرّب تعديل منتج من موبايلك وافتح الموقع من جهاز تاني — التعديل لازم يظهر فورًا.

---

## أشهر الأعطال وحلها 🔧

| اللي هتشوفه | السبب | الحل |
|---|---|---|
| `D1 binding "DB" is missing` | الداتابيز مش مربوطة بالمشروع، أو مربوطة على Preview بس، أو ماعملتش redeploy بعد الربط | الخطوة 3 بالكامل (Production + Retry deployment) |
| `no such table` / `Missing tables` | الجداول ما اتعملتش على الداتابيز البعيدة | الخطوة 2 (`--remote`) |
| `products: 0` / الشريط أخضر بس المنتجات من الجهاز | الجداول فاضية | الخطوة 4 (Load Default Catalog) |
| الشريط أحمر: Local Mode | أي سبب من اللي فوق — اقرا رسالة الخطأ اللي تحت الشريط | حسب الرسالة |
| `wrangler` بيطلب login | أوامر `--remote` محتاجة تسجيل دخول | `npx wrangler login` الأول |
| التعديلات بتظهر على جهاز واحد بس | الجهاز ده في الوضع المحلي (الداتابيز مش متصلة عنده — غالبًا كاش قديم أو API واقع) | افتح `/api/health` من نفس الجهاز وشوف الرسالة |

## ملحوظات

- الموقع **مصمم يشتغل حتى لو الداتابيز واقعة** (بيقع على نسخة محلية) —
  فكون الموقع بيفتح مش معناه إن الداتابيز شغالة. الفيصل هو `/api/health` وشريط الأدمن.
- بيانات التجربة المحلية (`npm run dev:cf`) محفوظة في `.wrangler/state` على جهازك
  ومالهاش أي علاقة بداتابيز السحابة.
- أمر `db:seed:remote` **بيمسح الجداول ويعيد إنشاءها** — ما تنفذوش لو عندك طلبات حقيقية
  على السحابة إلا لو واخد نسخة احتياطية.
